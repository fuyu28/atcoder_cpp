#include <bits/stdc++.h>
using namespace std;

#define all(x) x.begin(), x.end()
#define sz(x) (int)(x).size()
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define Gralph vecotr<vecotr<int>>
typedef long long ll;

int main() {
    ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    
    ll Sx, Sy;
    ll Tx, Ty;
    cin >> Sx >> Sy >> Tx >> Ty;
    ll ans=0;

    cout << ans << endl;
    return 0;
}