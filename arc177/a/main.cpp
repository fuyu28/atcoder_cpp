#include <bits/stdc++.h>
using namespace std;

#define all(x) x.begin(), x.end()
#define sz(x) (int)(x).size()
typedef long long ll;

int main() {
    ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);

    int A, B, C, D, E, F, N;
    cin >> A >> B >> C >> D >> E >> F >> N;
    vector<int> X(N);

    ll sum = 1 * A + 5 * B + 10 * C + 50 * D + 100 * E + 500 * F;

    for (auto& v : X) cin >> v;

    for (int i = 0; i < N; ++i) {
        
    }

    return 0;
}